apiVersion: v1
kind: Service
metadata:
  name: ingestion-service
  namespace: orderflow
  labels:
    app: ingestion-service
spec:
  selector:
    app: ingestion-service
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8000
  type: ClusterIP