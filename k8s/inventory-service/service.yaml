apiVersion: v1
kind: Service
metadata:
  name: inventory-service
  namespace: orderflow
  labels:
    app: inventory-service
spec:
  selector:
    app: inventory-service
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8000
  type: ClusterIP