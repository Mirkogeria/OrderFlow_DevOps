apiVersion: v1
kind: Service
metadata:
  name: genai-service
  namespace: orderflow
spec:
  type: {{ .Values.service.type }}
  selector:
    app: genai-service
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}